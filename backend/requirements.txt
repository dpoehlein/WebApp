# FastAPI + ASGI server
fastapi==0.110.0
uvicorn[standard]==0.27.1

# MongoDB (with async support via Motor)
pymongo==4.6.3
motor==3.4.0

# Data modeling and validation
pydantic==2.6.3
pydantic-core==2.16.3

# Environment variable support
python-dotenv==1.0.1

# OpenAI SDK
openai==1.13.3

# UUIDs (used in frontend but safe to include)
uuid==1.30

# (Optional: debugging tools)
httpx==0.27.0
